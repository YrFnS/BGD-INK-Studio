# Keep the browser-only prototype adapter enabled by default.
VITE_PLATFORM_DATA_SOURCE=local-prototype

# Set VITE_PLATFORM_DATA_SOURCE=frappe and provide the public Frappe origin
# only after the documented API endpoints and durable artwork uploads exist.
VITE_API_BASE_URL=
